# SQLite DSN. A relative path is resolved from the process working directory.
DATABASE_URI=data/app.db

# TCP port exposed by the HTTP server.
SERVER_PORT=3000

# Required HMAC secret for admin session tokens. Use a long random value.
JWT_SECRET=replace-with-a-long-random-secret
