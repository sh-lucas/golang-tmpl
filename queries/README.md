# Auto-generated

This entire folder is autogenerated and **should** be committed.    
SQLC generates it from the .sql files in `internal/features/` folder, and **editing it is entirely useless**.   

The project is data-centric and database-centric. So write the damn SQL and import it into your handlers; avoid raw SQL queries inside golang functions and it is not a problem to use data transformation inside SQL queries.
