version: "2"
sql:
  - engine: "sqlite"
    queries:
      - "internal/features/admins/admins_queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "queries"
        out: "queries"
        sql_package: "database/sql"
        emit_empty_slices: true
        emit_json_tags: true
